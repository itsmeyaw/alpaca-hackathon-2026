Review only the newly added protected-bracket validation fix in these current worktree locations: src/catalyst_router/domain.py BrokerOrderSnapshot active protection fields; src/catalyst_router/adapters/alpaca.py _order_snapshot active_legs logic; src/catalyst_router/execution.py active_broker_order protection check; tests/unit/test_execution.py ProtectedPositionBroker and nested-protection test; tests/unit/test_alpaca.py nested-leg test. The user asked to diagnose and fix a remote no-trade condition. Production evidence: an XLE bracket had both stop and take-profit legs active, but the old `snapshot.positions and len(snapshot.open_orders) < 2` heuristic falsely halted and flattened it. Look for correctness bugs, edge cases, behavioral regressions, and missing tests. Do not edit files. Return findings ordered by severity with file/line references, or explicitly say no findings; under 300 words.